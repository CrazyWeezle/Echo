DATABASE_URL=postgresql://echo:echo@db:5432/echo
JWT_SECRET=change-me

# Email verification / SMTP (dev defaults use MailHog)
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=no-reply@echo.local
PUBLIC_WEB_URL=http://localhost:3000

# Human verification (optional; implement later)
# TURNSTILE_SECRET=

# CORS: allowed origins for API and WebSocket (comma-separated)
# Local dev:
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
# Cloudflare Tunnel public URL (example):
# ALLOWED_ORIGINS=https://app.echologin.org
