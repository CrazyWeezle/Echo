TUNNEL_TOKEN=put-your-cloudflare-tunnel-token-here

# This token is created in Cloudflare Zero Trust → Networks → Tunnels → Create Tunnel → "Use a token".
# With token mode, hostnames are managed in the Cloudflare dashboard.
# In your tunnel, add a Public Hostname for:
#   app.echologin.org  →  http://web:80

